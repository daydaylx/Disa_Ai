Fokus: „Alte Version auf Cloudflare Pages“.
Lies: package.json (scripts), vite.config.*, public/_headers, public/_redirects.
Ziel:
- Einheitliche Toolchain (npm vs pnpm), Build-Script klarstellen.
- BASE_URL/VITE_BASE_URL sauber setzen.
- SPA-Fallback prüfen (Cloudflare: _redirects 200!).
- Service-Worker/Caching-Header auf Stale-Probleme prüfen.
Output:
- Minimaldiffs, jeweils mit Begründung.
- Test: `npm run build` muss clean durchlaufen.
- Danach PR-Vorlage generieren.
