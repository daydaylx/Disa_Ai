import React from "react";

/**
 * PersonaQuickBar (disabled)
 * Wir lassen die API/Imports intakt, geben aber kein UI aus,
 * damit keine defekten Schnellzugriffe mehr auftauchen.
 */
export default function PersonaQuickBar(): JSX.Element | null {
  return null;
}
