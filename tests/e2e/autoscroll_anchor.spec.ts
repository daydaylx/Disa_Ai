import { test } from "@playwright/test";

test.describe("Autoscroll anchor", () => {
  test.skip("Scroll-Anker wird separat abgedeckt", () => {
    /* Altlast: Feature wird derzeit refaktoriert, daher Test vorübergehend deaktiviert. */
  });
});
