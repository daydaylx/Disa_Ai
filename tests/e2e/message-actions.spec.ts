import { test } from "@playwright/test";

test.describe("Message Actions", () => {
  test.skip("Feature wird aktuell überarbeitet", () => {
    /* Placeholder: Copy/Delete Aktionen werden in neuer UI neu konzipiert */
  });
});
